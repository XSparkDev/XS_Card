# Project-wide Gradle settings.

# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
# Default value: -Xmx512m -XX:MaxMetaspaceSize=256m
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m

# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
# org.gradle.parallel=true

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true

# Enable AAPT2 PNG crunching
android.enablePngCrunchInReleaseBuilds=true

# Use this property to specify which architecture you want to build.
# You can also override it from the CLI using
# ./gradlew <task> -PreactNativeArchitectures=x86_64
reactNativeArchitectures=armeabi-v7a,arm64-v8a,x86,x86_64

# Use this property to enable support to the new architecture.
# This will allow you to use TurboModules and the Fabric render in
# your application. You should enable this flag either if you want
# to write custom TurboModules/Fabric components OR use libraries that
# are providing them.
newArchEnabled=false

# Use this property to enable or disable the Hermes JS engine.
# If set to false, you will be using JSC instead.
hermesEnabled=true

# Enable GIF support in React Native images (~200 B increase)
expo.gif.enabled=true
# Enable webp support in React Native images (~85 KB increase)
expo.webp.enabled=true
# Enable animated webp support (~3.4 MB increase)
# Disabled by default because iOS doesn't support animated webp
expo.webp.animated=false

# Enable network inspector
EX_DEV_CLIENT_NETWORK_INSPECTOR=true

# Use legacy packaging to compress native libraries in the resulting APK.
expo.useLegacyPackaging=false

# Fix for Windows path length issues
org.gradle.caching=true
org.gradle.parallel=true
org.gradle.configureondemand=true
org.gradle.daemon=true

# Reduce path length for Windows builds
android.enableR8.fullMode=false
android.useFullClasspathForDexingTransform=true

# CMake configuration for shorter paths
android.experimental.enableNewResourceProcessing=true

# React Native Reanimated specific fixes
react-native-reanimated.enableScreens=true
react-native-reanimated.enableScreens=false

# Optimize release builds to avoid EPERM and speed up builds
android.enableSeparateBuildPerCpuArchitecture=false
android.bundleInDebug=false
android.bundleInRelease=true
org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=512m -XX:+HeapDumpOnOutOfMemoryError
org.gradle.workers.max=2

# Force shorter build paths for Windows
android.experimental.enableNewResourceProcessing=true
android.enableR8.fullMode=false
android.useFullClasspathForDexingTransform=true

# Disable react-native-reanimated for release builds to avoid path length issues
react-native-reanimated.enableScreens=false
react-native-reanimated.enableScreens=false

# Completely disable react-native-reanimated for all builds on Windows
react-native-reanimated.enabled=false
react-native-reanimated.enableScreens=false
REACT_NATIVE_REANIMATED_DISABLE_AUTOLINK=1
android.defaults.buildfeatures.buildconfig=false
android.enableJetifier=true

# Support 16KB memory page sizes (Android requirement by Nov 1, 2025)
android.experimental.enableNewResourceProcessing=true
android.enableR8.fullMode=false
android.bundleInRelease=true
android.bundleInDebug=false
